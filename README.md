# seiryokukai_php

Gestionale palestra in PHP.

## Avvio locale

1. Configura MAMP/Apache puntando alla cartella `public`.
2. Copia `config/.env.example` in `config/.env` e aggiorna i parametri DB.
3. Apri il progetto nel browser.
